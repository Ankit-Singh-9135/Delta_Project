<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit yout chat</title>
</head>

<body>
    <p>Edit your chat: <%= chats._id %> </p>
    <p>This chat was sent from <b> <%= chats.from %> </b> to <b><%= chats.to %></b></p>
    <form method="POST" action="/chats/<%= chats._id %>?_method=PUT">
        <textarea name="msg" rows="5" cols="10"><%= chats.msg %></textarea>
        <button>edit</button>
    </form>
</body>

</html>